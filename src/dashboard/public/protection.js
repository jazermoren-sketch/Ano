const $=s=>document.querySelector(s);const guildId=document.body.dataset.guildId;async function request(path,body){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d}async function save(){const whitelist=$('#whitelist').value.split(',').map(x=>x.trim()).filter(Boolean);await request(`/api/servers/${guildId}/protection`,{antiNuke:$('#antiNuke').checked,antiRaid:$('#antiRaid').checked,raidJoinLimit:Number($('#raidLimit').value)||8,whitelist});$('#protectionStatus').textContent='Protection settings saved.'}document.addEventListener('DOMContentLoaded',()=>$('#saveProtectionAdvanced')?.addEventListener('click',()=>save().catch(e=>$('#protectionStatus').textContent=e.message)));